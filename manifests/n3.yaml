apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-hpa
  namespace: default
  labels:
    app: web-app
spec:
  minReplicas: "2"
  maxReplicas: "10"
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web-app
